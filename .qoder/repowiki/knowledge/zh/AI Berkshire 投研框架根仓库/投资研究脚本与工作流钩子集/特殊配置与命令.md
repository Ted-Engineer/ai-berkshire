`scripts/install-codex-prompts.sh` 会先运行 `python scripts/sync-codex-prompts.py` 再生成 `codex-prompts/*.md` 到 `$CODEX_HOME/prompts`；`scripts/sync-codex-skills.py --check` 用于 CI 中校验 `codex-skills/` 是否与 `skills/`` 保持同步；`tests/test_financial_rigor.py` 通过 `python tests/test_financial_rigor.py` 在 GBK 编码下验证财务工具的 Unicode 健壮性。