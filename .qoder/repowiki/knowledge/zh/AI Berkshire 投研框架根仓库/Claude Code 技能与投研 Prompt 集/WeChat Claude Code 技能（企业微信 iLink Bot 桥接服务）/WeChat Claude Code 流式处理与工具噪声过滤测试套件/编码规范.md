- 每个被测函数对应一个独立的 `.test.ts` 文件，按功能域（provider / turn-router / tool-noise-filter）组织测试用例。
- 测试通过构造最小化的状态对象或路由器实例（如 `freshState()`、`newRouter()`）隔离副作用，再断言回调输出或状态变化。
- 数据驱动的用例集中在 `fixtures/tool-noise/real-cases.json` 中，由 `extract-fixtures.mjs` 从真实日志抽取并维护，测试侧只做断言而不硬编码样本。
- 边界场景（空行、非法 JSON、长度恰好 400、无 stop_reason、无尾部可提取文本等）以独立 test 块显式覆盖。