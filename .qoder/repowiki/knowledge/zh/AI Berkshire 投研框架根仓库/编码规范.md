- 所有 Skill 的工作流定义集中在 `skills/*.md`，其他客户端目录均为自动生成产物，禁止手动编辑。
- 财务与估值计算统一调用 `python3 tools/financial_rigor.py` 子命令，禁止 LLM 心算或浮点运算。
- 关键金融数据必须来自至少两个独立来源并进行交叉验证，低置信度结论需明确标注。
- 研究报告统一输出至 `reports/` 目录，变更工具或 Skill 时不得改写无关的历史报告。
- 多客户端兼容性要求工具路径保持 `~/ai-berkshire/tools/...` 形式的相对 checkout 路径。