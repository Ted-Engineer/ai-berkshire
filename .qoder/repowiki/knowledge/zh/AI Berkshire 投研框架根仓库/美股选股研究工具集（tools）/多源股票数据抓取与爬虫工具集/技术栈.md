requests + urllib3（Yahoo Finance，禁用 SSL 校验）；yfinance（美股二次封装）；urllib.request + 正则（Finviz/StockAnalysis HTML 抓取）；Playwright async API（雪球爬虫，headless/headful 双模式）；curl 子进程（A 股绕过系统代理直连腾讯/东方财富）；FinMind REST API（台股，token 可选）。