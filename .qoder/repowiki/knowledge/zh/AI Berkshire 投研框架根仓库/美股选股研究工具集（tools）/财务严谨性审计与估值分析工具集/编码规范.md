- 所有金额与比率计算统一通过`Decimal(str(...))` + 自定义`Context(prec=28, ROUND_HALF_EVEN)`完成，禁止直接使用float做财务运算。
- 每个脚本自包含一个`main()`并通过`argparse`子命令暴露CLI接口，便于被Claude Code Skills或其他流程直接调用。
- 网络请求统一设置`User-Agent`为`ai-berkshire-research`或`AI-Berkshire Research research@example.com`，并使用`urllib3.disable_warnings()`关闭SSL校验以兼容内网环境。
- SEC XBRL解析统一通过`companyfacts` JSON接口，按`form in ('10-K','10-K/A')`且`fp=='FY'`过滤完整财年，再按`end`日期去重取最近若干条。
- 报告数据抽取采用正则匹配Markdown表格行与KV行，统一支持多种正负号（ASCII `-`、Unicode `−`/`–`/全角`－`）与逗号分隔数字，并通过`_is_valid_label`白名单过滤噪声标签。
- 结果输出固定为三段式表格：标题分隔线 + 对齐表头 + 数据行，并在末尾打印汇总统计（PASS/WARN/FAIL计数、排序排名、保存JSON路径）。