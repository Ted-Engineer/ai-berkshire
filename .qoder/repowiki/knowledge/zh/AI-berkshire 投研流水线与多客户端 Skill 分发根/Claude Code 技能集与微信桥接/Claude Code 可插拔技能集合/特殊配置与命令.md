wechat-claude-code 需先 `npm install`，再执行 `npm run setup` 扫码绑定微信账号，随后用 `npm run daemon -- start|stop|restart|status` 管理守护进程；open-websearch 支持本地 CLI/daemon（`open-websearch serve`、`open-websearch status`）与 MCP 两种接入路径，安装前需确认 npm 代理/镜像设置。