`wechat-claude-code` 需先 `npm install`（触发 postinstall → tsc 编译），再通过 `npm run daemon` 或 `node dist/main.js start` 启动；`setup-matt-pocock-skills` 需在首次使用其他工程技能前执行，用于生成 `docs/agents/issue-tracker.md`、`domain.md`、`triage-labels.md` 等约定文件；投研流程要求在执行后调用 `python tools/financial_rigor.py verify-market-cap|verify-valuation|three-scenario` 及 `python tools/report_audit.py extract|verdict` 完成数据交叉验证与抽检。