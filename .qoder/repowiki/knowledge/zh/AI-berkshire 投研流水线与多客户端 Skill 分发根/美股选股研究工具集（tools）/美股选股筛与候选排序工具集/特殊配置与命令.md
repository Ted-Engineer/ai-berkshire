运行前需确保 `data/` 目录存在以存放 fundamentals.json、watchlist.json 及扫描结果；Yahoo Finance 请求需先访问 fc.yahoo.com/finance.yahoo.com 获取 crumb 再调用 v10 quoteSummary；部分脚本通过 `os.environ['PYTHONHTTPSVERIFY']='0'` 关闭证书校验；finviz 扫描使用分页参数 `r` 每次递增 20 条。