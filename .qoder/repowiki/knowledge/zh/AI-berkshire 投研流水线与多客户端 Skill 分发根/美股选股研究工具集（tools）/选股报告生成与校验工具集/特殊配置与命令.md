运行前需确保存在 `data/finalist_detail.json` 等输入文件；多数脚本会向 `data/` 写中间结果（如 `data/hist_pe.txt`、`data/ttm_fix.json`、`data/xverify.json`）并修改 `reports/50pct-upside-final-20260808.md`；`log-command.sh` 用于记录实际执行的命令以便复现。