Node.js (ESM) 脚本用于美股全市场扫描：`pipeline.mjs` 调用 `api.nasdaq.com` 获取科技板块股票列表，爬取 Finviz 估值/性能页面并通过正则解析 HTML 表格，输出 `data/universe-tech.json`、`screened.json`、`finviz-merged.json`、`merged.json` 与 `rounds.jsonl`；其余内容均为 Markdown + JSON 文档。