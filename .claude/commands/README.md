# Live Claude Code commands snapshot (来源 skills/*.md + 本地 save-progress)
# 规范源是 skills/*.md（install-claude-commands.sh 从那里安装到 ~/.claude/commands）。
# 本目录用于版本化仓库内的命令副本；刷新方式：cp skills/*.md .claude/commands/
